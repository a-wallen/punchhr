import 'package:flutter/material.dart';

// TODO: Integrate Flutter QR Scanner
class ScanView extends StatelessWidget {
  const ScanView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Scan.."),
    );
  }
}
